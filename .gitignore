node_modules/
.env
*.log
.ada/

# ada session transcripts / local data
.cos0/

.dev.vars
.wrangler/

ada-auth.db*
# `screenshot` with no path writes here, so `npm run check:browser` leaves one in the repo root -
# and `npm run release` refuses to tag a dirty tree. It blocked two releases before this line.
page.png
runs/

# subagent-driven-development scratch
.superpowers/
  
# the bridge's shared secret, generated per machine  
extension/token.txt  
tmp-*.txt  
tmp-*.ts  
